name: Build Alpha

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: never

jobs:
  build-alpha:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12.10"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.18.0"
          cache: "npm"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - run: npm ci
      - run: python -m uv sync --frozen

      - name: Canonical alpha build
        run: |
          $ErrorActionPreference = "Stop"
          .\scripts\build-alpha.ps1 -SkipTests

      - name: Upload sidecar artifact
        uses: actions/upload-artifact@v4
        with:
          name: sidecar-canonical
          path: sidecar/dist/sidecar.exe

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: sentinel-alpha-installer
          path: src-tauri/target/release/bundle/nsis/*.exe

      - name: Upload manifest
        uses: actions/upload-artifact@v4
        with:
          name: alpha-manifest
          path: artifacts/alpha-manifest.json
